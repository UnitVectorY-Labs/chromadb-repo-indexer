# Remote Integration Fixture

This deterministic fixture verifies repository synchronization against the configured Chroma service.

## Safety

The test uses an isolated organization, repository, and branch namespace.
